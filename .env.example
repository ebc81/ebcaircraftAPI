# Copy this file to .env and fill in your values.
# .env is listed in .gitignore and will never be committed.

# Server
PORT=8080

# MySQL database credentials
DB_HOST=localhost
DB_USER=dump1090User
DB_PASSWORD=your_password_here
DB_NAME=dump1090
